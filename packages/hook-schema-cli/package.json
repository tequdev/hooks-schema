{
  "name": "hook-schema-cli",
  "version": "0.1.0",
  "description": "Command-line decoder for Xahau Hooks state schemas.",
  "type": "module",
  "bin": {
    "hook-schema": "./dist/main.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "state-schema": "workspace:*"
  }
}
