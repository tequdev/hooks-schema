{
  "name": "hooks-schema-cli",
  "version": "0.1.0",
  "description": "Command-line decoder for Xahau Hooks state schemas.",
  "type": "module",
  "bin": {
    "hooks-schema": "./build/main.js"
  },
  "files": ["build"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "hooks-schema": "workspace:*"
  }
}
